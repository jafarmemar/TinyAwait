name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  host:
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Select compiler
        run: |
          if [ "${{ matrix.compiler }}" = gcc ]; then
            echo "CXX=g++" >> "$GITHUB_ENV"
          else
            echo "CXX=clang++" >> "$GITHUB_ENV"
          fi
      - name: Configure
        run: cmake -S . -B build -DCMAKE_CXX_COMPILER="$CXX" -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure

  allocator-variants:
    strategy:
      fail-fast: false
      matrix:
        compiler: [g++, clang++]
        optimization: [-O0, -O2, -Os]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build and run allocator-focused tests
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/tinyawait-allocator-tests"
          for test in test_frame_pool test_freelist test_variable_frames test_legacy_config; do
            "${{ matrix.compiler }}" \
              -std=c++20 "${{ matrix.optimization }}" \
              -Wall -Wextra -Wpedantic -Werror \
              -fno-exceptions -fno-rtti \
              -Isrc "tests/${test}.cpp" \
              -o "$RUNNER_TEMP/tinyawait-allocator-tests/${test}"
            "$RUNNER_TEMP/tinyawait-allocator-tests/${test}"
          done

  sanitizers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Configure ASan/UBSan
        run: >-
          cmake -S . -B build-sanitize
          -DCMAKE_CXX_COMPILER=g++
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"
      - name: Build
        run: cmake --build build-sanitize --parallel
      - name: Test
        env:
          ASAN_OPTIONS: detect_leaks=1
        run: ctest --test-dir build-sanitize --output-on-failure

  size-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Size-optimized build
        run: |
          g++ -std=c++20 -Isrc -Os -flto -ffunction-sections -fdata-sections -fno-exceptions -fno-rtti \
            benchmarks/size_tinyawait.cpp -Wl,--gc-sections -o "$RUNNER_TEMP/tinyawait-size"
          size "$RUNNER_TEMP/tinyawait-size"

  arduino-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: arduino/setup-arduino-cli@v2
      - name: Show Arduino CLI version
        run: arduino-cli version
      - name: Install Arduino-ESP32 3.3.11 compile target
        run: |
          arduino-cli core update-index --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
          arduino-cli core install esp32:esp32@3.3.11 --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
      - name: Compile Arduino examples
        run: |
          for example in SingleDelay RepeatingDelay SequentialDelays NestedDelay; do
            arduino-cli compile \
              --fqbn esp32:esp32:esp32 \
              --library "$GITHUB_WORKSPACE" \
              "examples/${example}"
          done
